[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"

# SvelteKit SSR functions are generated by adapter-netlify
# No explicit functions dir needed; adapter outputs to .netlify/functions-internal
